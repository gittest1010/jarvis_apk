name: voice_assistant
description: "A smart voice assistant app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Voice-to-Text package
  speech_to_text: ^6.6.1

  # Text-to-Speech package
  flutter_tts: ^4.0.2

  # Permissions handle karne ke liye
  permission_handler: ^11.3.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
