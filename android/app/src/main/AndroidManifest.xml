<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== PERMISSIONS (Jarvis SRS) ===== -->
    
    <!-- Picovoice (Hotword + STT) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    
    <!-- Wakelock (Keep Screen On) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <!-- Gemini API (Online Brain) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- Connectivity Check -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Calling Features -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    
    <!-- installed_apps Package (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
    
    <!-- ===================================== -->


    <!-- ===== QUERIES (App Discovery) ===== -->
    <queries>
        <!-- For installed_apps package to find launcher apps -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
        
        <!-- For opening apps -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
        </intent>
        
        <!-- For phone calls -->
        <intent>
            <action android:name="android.intent.action.CALL"/>
        </intent>
    </queries>
    <!-- ==================================== -->


    <application
        android:label="Jarvis SRS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            
            <!-- Make this app a Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
            
    </application>
    
</manifest>