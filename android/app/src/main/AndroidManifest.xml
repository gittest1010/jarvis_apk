<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== NAYI PERMISSIONS (Jarvis ke liye) ===== -->
    <!-- Picovoice (Hotword + STT) ke liye -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Picovoice ko background mein chalaane ke liye -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <!-- Gemini API (Online Brain) ke liye -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Internet check karne ke liye -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Calling features ke liye -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!-- ============================================== -->


    <!-- ===== NAYI QUERIES (Jarvis ke liye) ===== -->
    <queries>
        <!-- flutter_tts (Offline bolne) ke liye -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- device_apps (App Drawer) ke liye -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
    <!-- ====================================== -->

    <application
        android:label="Jarvis srs" <!-- NAAM UPDATE KIYA GAYA -->
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity" <!-- Naya (Regenerated) activity naam -->
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <!-- ===== YEH APP KO LAUNCHER BANATA HAI ===== -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- ========================================== -->

        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>