<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== PERMISSIONS (Jarvis srs ke liye) ===== -->
    <!-- Picovoice (Hotword + STT) ke liye -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Picovoice/Wakelock (Background + Screen) ke liye -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <!-- Gemini API (Online Brain) ke liye -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Internet check karne ke liye -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Calling features ke liye -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!-- ============================================== -->


    <!-- ===== QUERIES (Jarvis srs ke liye) ===== -->
    <queries>
        <!-- device_apps (App Drawer) ke liye -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        
        <!-- HATA DIYA GAYA: flutter_tts (Offline bolne) ke liye zaroori tha -->
        <!-- 
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent> 
        -->
    </queries>
    <!-- ====================================== -->

    <application
        android:label="Jarvis srs"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            androidG:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <!-- App ko Launcher banata hai -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>